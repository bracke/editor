# Syntax Colouring

Current syntax-colouring behavior is owned by the parser-backed Ada language
model and the render cache. Routine development should use the relevant slice
from `docs/testing.md`; release validation uses the release checklist.

Historical syntax-colouring pass evidence is archived at
`docs/archive/active_doc_history/syntax_colouring.md`.
