# Ada Parser Coverage Matrix

The active Ada parser coverage source of truth is the parser implementation,
its AUnit coverage, and the `ada-language` test slice. Use
`tools/bin/unit_tests ada-language` for routine parser/language changes.

Historical coverage-matrix evidence is archived at
`docs/archive/active_doc_history/ada_parser_coverage_matrix.md`.
