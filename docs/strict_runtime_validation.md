# Strict Runtime Validation

Current strict runtime validation belongs to the release workflow. Use
`docs/release/RELEASE_CHECKLIST.md` and `tools/bin/release_commands` for the
current command sequence.

Historical strict-runtime validation notes are archived at
`docs/archive/active_doc_history/strict_runtime_validation.md`.
